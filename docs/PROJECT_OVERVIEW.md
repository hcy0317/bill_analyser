# Bill Analyser 项目总览

多来源账单导入 + 智能去重 + 自动分类 + 多维统计分析全栈系统。Rust 已成为主 HTTP 服务入口：`bill_http_server` 默认监听 `BILL_ANALYSER_HTTP_BIND=127.0.0.1:5000`，`start_backend.ps1` 先启动 Python/Flask sidecar（默认 `BILL_ANALYSER_API_PORT=5001`），再通过 `BILL_ANALYSER_PYTHON_UPSTREAM=http://127.0.0.1:5001` 反向代理尚未迁移的端点；`proxy_only` 模式仍可代理未接管的 `/api/*`，但 `import_db_runtime` 模式只允许 manifest 中标记为 `PythonProxied` 的端点进入 Python，未知 `/api/...` 不再静默透传。Rust runtime metadata 现在声明 `api_takeover=true`，`import_db_runtime` 模式的 `business_migration` 标注为 `import-db-runtime+bills-crud-runtime+bills-picture-runtime+bills-export-runtime+bills-recurring-runtime+bills-reconciliation-runtime+bills-category-actions-runtime+budgets-crud-execution-forecast-history-import-runtime+matching-recurring-calendar-networth-runtime+statistics-read-runtime+statistics-analyzer-runtime+statistics-exchange-runtime+taxonomy-accounts-runtime+taxonomy-tags-runtime+taxonomy-tags-batch-runtime+taxonomy-categories-runtime+taxonomy-templates-runtime+taxonomy-settings-bundle-runtime+ai-learning-center-runtime+ai-llm-config-candidates-runtime+ai-llm-provider-generation-runtime+ai-ocr-recognition-runtime+auth-login-register-token-session-personal-refresh-logout-account-recovery-oauth2-authorize-profile-cloud-external-auth-system-user-data-statistics-2fa-status-verify-recovery-write-step-up-export-clear-runtime+backup-ops-sync-runtime`。默认 `import_db_runtime` 模式下，Rust 直接接管 import v2 JSON parse/parse_generic/dedup/confirm、前端 FormData CSV/XLS/XLSX/HTML-xls 上传专用 parser 检测解析、未匹配文件 temp preview 与列映射 parse_generic、session 读取/清理、preview 分页读取、preview update/reclassify 显式 DB 更新、preview-item transfer/recurring 决策、learning 会话预览旁路与 promotion、LLM accept/reject/memory 事件、LLM config/configs/candidates 读写与候选审核、LLM provider 推荐/分析/规则归纳、全局 Learning Center suggestions/rules loop、OCR config app_settings 读写与 receipt OCR recognition，并接管核心账单/交易 CRUD 路由、交易图片上传/未使用清理、账单 CSV/XLSX 导出、账单 recurring candidates/match 路由、账户对账单路由（旧 Flask `bills/reconciliation.py` route shell 已删除）、账单分类 quick actions 路由（旧 Flask `bills/category_actions.py` route shell 已删除）、账户 CRUD/display-order/余额同步/交易迁移清空路由、标签 CRUD/display-order/batch-create 路由、分类主数据、分类账单统计与批量重分类路由（旧 Flask categories route package 已删除）、legacy 分类规则 config/cache、分类规则列表/创建/更新/删除/重排/defaults/migrate/测试与规则中心概览路由、设置包导入/预览/导出路由、模板 CRUD/display-order 路由、预算 CRUD/export/execution/forecast/history/snapshot/import 路由（旧 Flask `budgets/` route package 已删除）、DB-backed 统计读取路由（`GET /api/statistics/category-statistics`、`GET /api/statistics/category-statistics/trends`、`GET /api/statistics/asset-trends`、`GET /api/statistics/category-pie`、`GET /api/statistics/top-merchants`、`GET /api/statistics/amounts`）、Analyzer 统计路由（`GET /api/statistics/overview`、`GET /api/statistics/trends`、`GET /api/statistics/comparison`、`GET /api/statistics/category`、`GET /api/statistics/trend`）、洞察异常路由（`GET /api/insights/anomalies`）与汇率 REST 路由（`GET /api/statistics/exchange-rates`、`PUT /api/statistics/exchange-rates/custom`、`DELETE /api/statistics/exchange-rates/custom/{currency}`，旧 Flask statistics read/exchange/Analyzer/insights route shell 已删除），login/register/token/profile/user-data/2FA/step-up 认证安全路由，以及备份文件与任务调度路由（`GET /api/backup/`、`POST /api/backup/create`、`POST /api/backup/restore/verify`、`GET /api/backup/download/<filename>`、`DELETE /api/backup/delete/<filename>`、`POST /api/backup/restore/<filename>`、`POST /api/backup/cleanup`、`GET|POST /api/backup/jobs`、`POST /api/backup/sync`）。Rust backup ops runtime 会验证当前请求身份，直接创建/校验/下载/删除/恢复本地 `data/` zip 备份，公开 `.zip.enc` Fernet-compatible 加密备份名，按 `backup_records` 执行 record-first cleanup，并按 `user_id` 读写 `backup_jobs`；同一用户同一 `job_type` 保存会更新既有任务，跨用户或不存在的 `id` 不会被更新，所有 backup 关键动作都会 best-effort 写入包含 actor 与来源 IP 的审计。账户 CRUD/display-order/余额同步/交易迁移清空、标签 CRUD/display-order/batch-create、分类主数据/分类账单统计/批量重分类、legacy 分类规则 config/cache、分类规则列表/创建/更新/删除/重排/defaults/migrate/测试、规则中心概览、设置包导入/预览/导出与模板 CRUD/display-order HTTP 路由，账户/标签/分类/模板、设置包 taxonomy helper 与分类规则表达式编译/匹配等路径已逐步通过内部 Rust crates/bridge 接管，导入/学习/matching/recurring/budgets/statistics、auth login/register/token session/account recovery/profile cloud/external-auth/OAuth2 authorize/user-data statistics/export/clear/2FA status/2FA verify/2FA recovery verify/2FA write/step-up、交易图片上传/未使用清理、账户对账单、账单分类 quick actions、AI OCR/LLM provider/config、backup ops 与 backup/ops 安全合同等路径也在 Rust crate 中固定合同层；`bill-analyser-db` 已提供 SQLite runtime path guard、WAL/FK 连接配置、foundational schema 初始化、core legacy user-scope/unique 迁移、schema dry-run copy validation、导入 session、preview、parser template staging、StandardBill-to-parser-template adapter、parser-template-to-preview staging adapter、selected preview confirm-to-bills、preview update/reclassify、preview-item transfer/recurring/learning decision、learning promotion、preview LLM recommendation/memory event、全局 Learning Center suggestion mining/rule store、OCR config app_settings 与 annotation sample，以及账单 CRUD、交易图片上传/未使用清理、账单 CSV/XLSX 导出、账单 recurring candidates/match、账单分类 quick actions、账户 CRUD/display-order/余额同步/交易迁移清空、标签 CRUD/display-order/batch-create、分类主数据、分类账单统计、批量重分类、legacy 分类规则 config/cache、模板 CRUD/display-order、预算 CRUD/export/execution/forecast/history/snapshot/import、统计读取、Analyzer 聚合、洞察异常与汇率 custom rate 读写、login user lookup/failed-login/last-login、register user/default seed、token session list/revoke、API/MCP token session 写入、refresh token session 轮换、logout session 失效、account recovery email/password 更新、profile 更新、cloud settings upsert/delete、external-auth list/delete、user-data statistics/export/clear、2FA status flag read、2FA verify user lookup、2FA 恢复码 hash/replace/consume/clear/count、2FA enable/disable 原子写入、token 密码失败计数、auth log 写入、user-scoped backup jobs/records 读写、backup ops schema 初始化和本地备份文件 runtime 的 Rust SQLite 原语及回滚/user-scope/父子预算上卷合同测试。SQLCipher/encryption schema 仍由 Python sidecar 保留；备份文件 I/O 与云同步已由 Rust backup ops runtime 接管；真实 LLM provider 推荐/分析/规则归纳（`POST /api/llm/preview-recommend`、`/analyze-transactions`、`/rule-synthesis`）、全局 Learning Center suggestions/rules 循环（`/api/learning/suggestions*`、`/api/learning/rules*`）与 receipt OCR recognition（`POST /api/ml/receipt-recognition`）已由 Rust 接管，旧 Flask `learning.py` route shell 已删除；OAuth provider exchange 在当前 workspace build 中是 Rust-owned disabled-safe/not-implemented 合同，不再保留 Python proxy；旧 Flask bills import route package 已删除；auth 剩余 Python 删除仍因残留引用继续阻塞；Python bills、budgets、statistics read/exchange/Analyzer 与 insights REST route shell 已删除。前端 Vue 3/TypeScript/Vite，数据库 SQLite WAL 模式。

已迁移的 Rust auth/profile/user-data/backup jobs 路由可用前端 Bearer access token 校验 HS256/HS384/HS512 HMAC JWT，并通过 user-scope DB helper 限定当前用户；备份文件类 Rust 路由在 Bearer session 之外还要求短期 `step_up` token，可信内部用户头仅作为 runtime bridge 通道。

P8b 当前新增一层 Rust-owned matching/recurring/calendar/networth 运行时：`import_db_runtime` 直接处理 `/api/matching/*` formal matching candidates、feedback、manual pairs、candidate accept/reject/clear、reconciliation candidates、reconcile-history 与 retired investment settings，同时继续处理 `GET /api/recurring/suggestions`、`POST /api/recurring/suggestions/detect`、`POST /api/recurring/suggestions/{suggestion_id}/accept`、`POST /api/recurring/suggestions/{suggestion_id}/reject`、`GET /api/calendar/events` 与 `GET /api/networth/snapshot`。formal matching 通过 Rust `bill-analyser-db/src/matching.rs` 直接读写 `bill_pair_links`、`bill_pair_feedback`、transfer/investment/learning suppressions、import preview action payload 与 reconciliation projections；旧 Flask `matching/`、`recurring.py`、`calendar.py` 与 `networth.py` route shell 已删除，matching/recurring/calendar/networth route set 不再有 Python-proxied 剩余项。

P12c 当前新增 Rust-owned backup sync 运行时：`import_db_runtime` 直接处理全部 backup REST 主链，包括备份列表、创建、恢复预验证、下载、删除、恢复、cleanup、`GET|POST /api/backup/jobs` 与 `POST /api/backup/sync`。Rust runtime 直接读写本地 `data/` / `backup/`、生成可恢复 zip、按 `BILL_ANALYSER_BACKUP_ENCRYPTION_KEY` 生成 Fernet-compatible 加密备份公开名、流式下载备份文件、恢复前创建唯一 `before_restore_*` 快照、基于 `backup_records` 做 retention cleanup，并写入 backup 相关审计；当 SQLite DB 位于 `data/` 内时，备份创建先用 `VACUUM INTO` 生成一致性 DB 快照，实际恢复先解压到 staging 目录再替换 `data/`。`POST /api/backup/sync` 会先创建本地备份，再按脱敏 provider config 上传到 OSS/S3/COS/Azure Blob/WebDAV，并把 object key、provider 和 sync 状态写入 `backup_records` metadata 与 `backup_cloud_synced` 审计；Python `SyncManager` 只作为 CLI/兼容残留保留，不再是默认 backup ops REST 运行时依赖。

P11 当前新增一层 Rust-owned LLM 配置、候选建议、provider 生成和 OCR recognition 运行时：`import_db_runtime` 直接处理 `GET|POST /api/llm/config`、`GET|POST /api/llm/configs`、`PUT|DELETE /api/llm/configs/{config_id}`、`POST /api/llm/configs/{config_id}/activate`、`GET /api/llm/candidates`、`GET /api/llm/candidates/{candidate_id}`、`POST /api/llm/candidates/{candidate_id}/accept|reject`、`POST /api/llm/preview-recommend`、`POST /api/llm/analyze-transactions`、`POST /api/llm/rule-synthesis`、`POST /api/ml/receipt-recognition` 与 `GET|PUT /api/ml/receipt-recognition/config`。`POST /api/llm/config` 的临时 provider 配置保存在 Rust 进程内 user-scoped runtime map，避免把临时 API key 写入 `app_settings`；saved config 仍落 `llm_configs` 并在响应里脱敏，rule 型候选 accept 会按匹配分类物化 `category_rules`。Rust provider 生成会写 preview 黄色推荐/memory event、`classification` / `rule_induction` / `rule_synthesis` 型 `llm_candidates`，并保留 provider alias/default、Azure endpoint 校验、explicit Base URL allowlist/SSRF 防护、advanced prompt settings、JSON 数组解析、provider 响应体上限、候选 raw 片段截断和 user-scoped rate limit；preview 推荐会按当前用户账户名重新解析账户 ID，`rule-synthesis` 只有存在 durable 学习证据时才调用 provider；OCR recognition 默认 disabled 返回 `provider_unconfigured`，`cloud_stub` 保持未配置占位，`tesseract` 通过外部 tesseract 二进制 stdin/stdout 执行并按支付截图解析合同返回金额、时间、描述、平台、provenance 与 confidence。

Rust bills CRUD/export/picture/recurring runtime 已直接负责核心账单列表、创建、查询、更新、删除、批量创建、导出、交易图片和正式 recurring match 路由，以及 legacy `POST /api/bills/modify` 与 `POST /api/bills/delete`；旧 Flask `bills/crud_prepare.py`、`bills/crud_create_update.py` 与 `bills/crud_query.py` route shell 已删除。旧 Flask bills import route package 已删除，Python sidecar 不再注册 `/api/bills`；导入、配置、学习、v2 session/preview/decision 与 import batch 等 `/api/bills/import*` 路径由 Rust import_db_runtime 直接接管。

Rust taxonomy tag runtime 负责 `GET|POST /api/tags/`、`GET|PUT|DELETE /api/tags/{tag_id}`、`POST /api/tags/batch` 与 `PUT /api/tags/display-orders`，旧 Flask `tags.py` route shell 已删除；Rust 按当前 Bearer 用户读写普通 SQLite 标签主数据，保留 `success/result` envelope、前端 `displayOrder`/`hidden` DTO、重复标签 409、`skipExists` 和显示排序语义。

Rust taxonomy template runtime 负责 `GET|POST /api/templates/`、`GET|PUT|DELETE /api/templates/{template_id}` 与 `PUT /api/templates/display-orders`，旧 Flask `templates.py` route shell 已删除；Rust 按当前 Bearer 用户读写普通 SQLite 交易模板与定时交易模板，保留 `success/result` envelope、`templateType` 双表筛选、前端模板 DTO、隐藏与显示排序语义。

Rust 解析器运行时已拆入 `crates/bill-analyser-parsers`，负责 parser registry metadata、`RawBill`/`StandardBill`、parser output normalization、`parser_tags` JSON 序列化与 golden fixture 合同，并提供微信、支付宝、工商银行、民生银行、农业银行、建设银行专用 CSV/XLS/XLSX/HTML-xls 导入解析。`bill-analyser-db` 和 `bill-analyser-http` 从该 crate 消费标准账单结构，`bill-analyser-core` 不再作为 parser 类型导出入口。浏览器上传的 parser-first multipart 入口（`POST /api/bills/import/v2/parse`、legacy `POST /api/bills/parse_import` 与 `POST /api/bills/import/upload`）由 Rust parser-first 路径执行专用 parser 检测和解析；Rust 继续直接处理 JSON import parse、未匹配文件列映射 `parse_generic`、session/preview/dedup/confirm 等数据库运行时。Python parser sidecar 仅作为迁移期历史实现与对照测试保留，不再是 parser-first 上传主链路。

Rust taxonomy category-rule runtime 负责 `GET|POST /api/category-rules/`、`PUT|DELETE /api/category-rules/{rule_id}`、`POST /api/category-rules/reorder`、`POST /api/category-rules/defaults`、`POST /api/category-rules/migrate`、`POST /api/category-rules/{rule_id}/test`、`GET /api/rules/overview`、`POST /api/categories/update-all` 与 legacy `GET|PUT /api/categories/rules`，其中规则中心概览旧 Flask route shell 与旧 Flask `category_rules.py` route shell 已删除；Rust 按当前 Bearer 用户读取或写入 `category_rules`、`categories`、`import_learning_rules`、`recurring_bills`、账单行与用户隔离 `app_settings` legacy rules cache，汇总规则中心概览并用 canonical 分类规则批量重分类账单；保留既有 `success/data` 或 `success/result` envelope、分类规则列表 `total`、`category_id` 筛选、`enabled_only=false` 显示禁用规则语义、创建/更新/删除/重排响应 envelope、默认分类/规则幂等补齐、legacy keywords 与投资关键词设置到 canonical 规则的幂等迁移、无写副作用的规则表达式测试响应、批量重分类 `force` 语义、legacy 分类规则 GET fallback 的旧 `CategoryEngine.rules` JSON 形状、legacy 分类规则 PUT 的旧 `rules` payload 持久化，以及 learning/category/recurring 规则计数。Rust taxonomy account runtime 负责 `GET|POST /api/accounts/`、`GET|PUT|DELETE /api/accounts/{account_id}`、`PUT /api/accounts/display-orders`、`POST /api/accounts/sync-balances`、`POST /api/accounts/{account_id}/transactions/move` 与 `POST /api/accounts/{account_id}/transactions/clear`，旧 Flask `accounts/` route package 已删除；Rust 按当前 Bearer 用户遍历账户，根据账单 source/destination 账户、初始余额和同账户投资盈亏修正重算 SQLite 元余额；账户交易迁移/清空会验证当前密码或操作密码回退，按 user-scope 更新/删除 bills、account_transfers 与账单副作用，重新同步账户余额并 best-effort 写入 account 审计日志。Rust taxonomy settings bundle runtime 负责 `GET /api/settings/bundle/export`、`GET|POST /api/settings/bundle/sections/{section_key}/export`、`POST /api/settings/bundle/import`、`POST /api/settings/bundle/import/preview`、`POST /api/settings/bundle/sections/{section_key}/import` 与 `POST /api/settings/bundle/sections/{section_key}/import/preview`，旧 Flask `settings_bundle.py` route shell 已删除；Rust 按当前用户导出账户、分类、标签、模板、分类识别规则、LLM 配置骨架与 OCR 配置，保留 JSON attachment 响应、LLM API Key 脱敏、敏感 section 当前密码校验、导入 preview 事务 rollback、跨 section upsert 顺序、外部 ref 不误绑定本地 ID、LLM masked secret 保留与 OCR app_settings 写入；Python CategoryEngine matcher cache 仅保留真实导入/学习流程中的未迁移匹配执行。

Rust taxonomy settings runtime 还直接负责无鉴权 `GET /api/settings/encryption/status`，返回 Flask-compatible `success/data` SQLCipher 状态投影，旧 Flask route shell 已删除；当前 Rust SQLite runtime 未启用 SQLCipher provider，因此即使环境请求加密也会报告 `sqlcipher_available=false` 与 `encrypted=false`，真实 SQLCipher 连接/迁移仍作为 Python/后续 ops 切面保留。

Rust profile runtime 会在邮箱变更时校验格式/唯一性、重置 `email_verified` 并记录审计日志；账户/分类引用 ID 必须属于当前用户，头像上传只接受 PNG/JPEG/GIF/WebP 真实图片内容，已接管的 profile/cloud/external-auth 路由直接响应 `OPTIONS` preflight。

Rust step-up runtime 要求当前密码、已配置的操作密码或有效 TOTP passcode 才签发 `step_up` action token。Rust user-data clear runtime 要求当前密码、已配置的操作密码或有效 `step_up` action token；step-up 与 user-data clear 失败认证会写入 auth log 并进入短窗口限流。全量清理覆盖当前用户的账单/预算/账户/分类/分类规则/标签/模板、导入 session/preview/template staging，以及 Rust 导入/学习运行时样本（`import_annotation_samples`、`llm_memory_events`），`user_data` 审计写入保持 best-effort 兼容并记录聚合 affected_count。

Rust transaction picture runtime 使用 `BILL_ANALYSER_UPLOADS_DIR`（默认 `data/uploads`）保存 multipart 上传文件并按安全文件名清理未使用图片。

Rust DB foundation 额外负责 auth/security schema 的幂等初始化，覆盖 `users` legacy 认证字段补齐、`sessions`、`auth_logs`、2FA 恢复码、external auth 与 profile cloud settings 基础表/索引；2FA 恢复码当前接管 hash/replace/consume/clear/count SQLite primitive，并由 Rust recovery verify 路由一次性消费；2FA 写管理路由当前由 Rust 生成 setup secret/QR、确认启用、禁用并重生成恢复码；step-up 验证当前由 Rust 通过当前密码、已配置的操作密码回退或 TOTP passcode 签发短期 action token；data export 与 data clear 当前由 Rust 按当前 Bearer 用户执行。

## 目录

- [项目定位](overview-positioning.md) — 系统目标、核心数据源（微信/支付宝/多家银行）
- [总体架构](overview-architecture.md) — 分层结构（API/Core/Database/Frontend）、异步桥接、REST 主链模式
- [后端模块](overview-backend.md) — 按功能域拆分的 API 路由包、Core 业务逻辑、解析器工厂、Database façade 与 mixin 包结构
- [前端模块](overview-frontend.md) — 视图（desktop/mobile）、Pinia stores、统一 axios 服务层、设置 JSON 导入导出入口
- [API 路由与 REST 收口](overview-api-routes.md) — Flask 蓝图注册、账户/标签/模板/预算/账单/分类/设置包/OCR/LLM 配置各域 REST 收口进展
- [导入链路](overview-import.md) — v2 三阶段导入、预览校验交互、图片 OCR 回填、学习域（corpus/dual-head model/overlay）、LLM 推荐
- [Matching 域](overview-matching.md) — 转账/投资/learning 配对候选、generic accept/reject/clear、manual pair 管理、reconcile-history
- [统计与汇率](overview-statistics.md) — 统计主链、汇率 REST、用户数据管理、legacy 收口条目
- [认证与安全](overview-auth-security.md) — 认证/2FA/token/backup/step-up/OAuth2 全链路 REST 收口与审计
- [数据库与数据流](overview-database.md) — 主要业务表、导入三阶段临时表、导入处理顺序
- [日志与运维](overview-ops.md) — 统一日志入口、启停脚本
- [测试结构](overview-testing.md) — 测试分布、结构体量门禁、推荐验证命令
