# Bill Analyser 认证与安全

- 认证入口主链已统一为 `POST /api/auth/login`、`POST /api/auth/register`、`POST /api/auth/logout`；旧 `authorize.json`、`register.json`、`logout.json` 已停止使用，并由 legacy 404 回归保护
- 认证辅助入口已继续收口到 REST：`POST /api/auth/email/verify`、`POST /api/auth/email/resend-verification`、`POST /api/auth/password/forgot`、`POST /api/auth/password/reset`；旧 `verify_email/*.json` 与 `forget_password/*.json` 已停止使用，并由 legacy 404 回归保护
- 用户资料主链已扩展到头像 REST：`GET|PUT /api/profile`、`POST|DELETE /api/profile/avatar`；旧 `v1/users/avatar/update.json` 与 `v1/users/avatar/remove.json` 已停止使用，并由 legacy 404 回归保护
- 用户资料主链已扩展到验证邮件重发 REST：`POST /api/profile/email/resend-verification`；旧 `v1/users/verify_email/resend.json` 已停止使用，并由 legacy 404 回归保护
- Rust profile runtime 对邮箱变更会校验格式/唯一性、重置 `email_verified` 并写入 `profile_email_changed` 审计日志；`defaultAccountId`、`cashAccountId`、`cashTransferCategoryId` 只接受当前用户名下的账户/分类 ID。
- Rust avatar runtime 只接受 PNG/JPEG/GIF/WebP 真实图片内容，multipart MIME 与文件魔数必须一致；验证邮件重发当前仍是 mock-success 审计记录，但带用户级短窗口限流。
- 用户资料主链已扩展到第三方登录 REST：`GET /api/profile/external-auths`、`POST /api/profile/external-auths/unlink`；旧 `v1/users/external_auth/*.json` 已停止使用，并由 legacy 404 回归保护
- 用户资料主链已扩展到应用云同步设置 REST：`GET|PUT|DELETE /api/profile/cloud-settings`；旧 `v1/users/settings/cloud/*.json` 已停止使用，并由 legacy 404 回归保护
- 2FA 主链已扩展到完整 REST 写接口：`GET /api/2fa/status`、`POST /api/2fa/enable/request`、`POST /api/2fa/enable/confirm`、`POST /api/2fa/disable`、`POST /api/2fa/recovery/regenerate`；旧 `v1/users/2fa/*.json` 写接口已停止使用，并由 legacy 404 回归保护
- 2FA 登录验证链路已补齐 REST：`POST /api/2fa/verify`、`POST /api/2fa/recovery/verify`；`/api/auth/login` 在用户启用 2FA 时返回 `need2FA` 与待验证 token，旧 `/api/2fa/authorize.json`、`/api/2fa/recovery.json` 已停止使用，并由 legacy 404 回归保护
- 2FA 恢复码当前已从纯内存缓存升级为数据库持久化哈希存储：`db.py` 新增 `user_two_factor_recovery_codes` 表，启用 2FA / 重生成恢复码时会整体替换当前批次，恢复码登录校验按哈希一次性消费，旧批次在重生成后立即失效
- 安全中心当前已补齐 step-up 验证基础：`POST /api/security/step-up/verify` 可通过当前密码、已配置的操作密码回退或 TOTP passcode 签发短期 `step_up` token，`POST /api/2fa/disable`、`POST /api/2fa/recovery/regenerate`、`POST /api/data/clear/transactions`、`POST /api/data/clear/all` 已支持使用 `stepUpToken` 替代重复提交密码
- Rust step-up 密码分支会先校验当前用户密码，再按已配置的 `BILL_ANALYSER_OPERATION_PASSWORD` 或 `app_settings.operation_password` 回退；2FA 写管理的直接密码分支仍保留 Python 的 operation password 兼容顺序，未配置操作密码时保持既有兼容放行语义。
- 用户数据管理当前在 `import_db_runtime` 模式下由 Rust 直接接管：`GET /api/data/statistics` 使用 Bearer access token 解析当前用户，并返回 Flask-compatible `success/result` 外壳中的账单、账户、分类、标签和模板数量；`GET /api/data/export.<file_type>` 按当前用户与筛选条件导出带 BOM 的 CSV/TSV；`POST /api/data/clear/transactions` 与 `POST /api/data/clear/all` 通过当前密码、已配置的操作密码回退或 `stepUpToken` 验证后执行用户级清理并写入 `user_data` 审计元数据。
- 用户数据清理的操作密码回退只在 `BILL_ANALYSER_OPERATION_PASSWORD` 或 `app_settings.operation_password` 已配置时生效；未配置操作密码时必须使用当前密码或有效 `stepUpToken`。step-up 与 user-data clear 失败认证会写入 `auth_logs` 并按短窗口限流；全量清理同时覆盖分类规则与 Rust-owned 导入/学习运行时样本（`import_annotation_samples`、`llm_memory_events`），`audit_logs` 缺失或写入失败时保持 best-effort 兼容，成功审计的 `affected_count` 使用清理计数聚合值。
- 备份主链当前所有列表、创建、恢复预验证、下载、删除、恢复、清理与 jobs 入口均要求认证；除创建 / 下载 / 删除 / 恢复外，还支持恢复前预验证与可选本地加密：`GET /api/backup/` 与 `POST /api/backup/create` 会返回备份 `checksum`、压缩包结构检查结果与 `ready_to_restore` 摘要，恢复成功条件要求 `valid_zip=true` 且包含 `data/` 目录；当存在 `BILL_ANALYSER_BACKUP_ENCRYPTION_KEY` 时，创建接口会产出 `.zip.enc` 本地加密备份并在记录中标记 `encrypted=true`；`POST /api/backup/restore/verify` 与 `POST /api/backup/restore/<filename>` 会自动识别并解密本地加密备份；`POST /api/backup/cleanup` 现已同时覆盖 `.zip` / `.zip.enc`，并优先基于 `backup_records` 决定 retention 淘汰项与回写 `deleted` 状态
- 认证域敏感安全动作当前会同时写认证日志与操作审计：`2fa_enabled`、`2fa_disabled`、`2fa_recovery_regenerated`、`2fa_recovery_code_used` 通过 `audit_logs` 持久化关键安全事件上下文
- 备份域关键动作当前也会写入 `audit_logs`：`backup_created`、`backup_deleted`、`backup_restored`、`backup_restore_verified` 会记录备份文件名、校验值、预验证结果或失败原因，便于后续追踪恢复链路与异常排查
- OAuth2 callback authorize 已收口到 `POST /api/auth/oauth2/authorize`；旧 `/api/oauth2/authorize.json` 已停止使用，并由 legacy 404 回归保护。Rust auth runtime 当前直接提供 disabled-safe / not-implemented 语义，当前 workspace build 不存在真实 provider exchange 或 Python proxy remainder
- token 会话主链已切到认证域 REST：`GET|DELETE /api/tokens`、`POST /api/tokens/api`、`POST /api/tokens/mcp`、`POST /api/tokens/refresh`、`DELETE /api/tokens/<id>`；旧 `v1/tokens/generate*.json`、`v1/tokens/revoke*.json` 与 `v1/tokens/refresh.json` 已收口到新主链，并由 legacy 回归保护
- 认证错误契约已补齐：`POST /api/auth/login` 与 `POST /api/tokens/refresh` 在请求体为空、`null` 或其他非对象 JSON 时返回 `400 Invalid request`，不再落入 `500`。
- Rust 迁移当前已具备认证安全核心 foundation，并在 `import_db_runtime` 模式下直接接管 `POST /api/auth/login`、`POST /api/auth/register`、`GET /api/tokens`、`DELETE /api/tokens`、`DELETE /api/tokens/{token_id}`、`POST /api/tokens/api`、`POST /api/tokens/mcp`、`POST /api/tokens/refresh`、`POST /api/auth/logout`、`POST /api/auth/email/verify`、`POST /api/auth/email/resend-verification`、`POST /api/auth/password/forgot`、`POST /api/auth/password/reset`、`POST /api/auth/oauth2/authorize`、`GET|PUT /api/profile`、`POST|DELETE /api/profile/avatar`、`POST /api/profile/email/resend-verification`、`GET|PUT|DELETE /api/profile/cloud-settings`、`GET /api/profile/external-auths`、`POST /api/profile/external-auths/unlink`、`GET /api/system/version`、`GET /api/data/statistics`、`GET /api/data/export.<file_type>`、`POST /api/data/clear/transactions`、`POST /api/data/clear/all`、`POST /api/security/step-up/verify`、`GET /api/2fa/status`、`POST /api/2fa/verify`、`POST /api/2fa/enable/request`、`POST /api/2fa/enable/confirm`、`POST /api/2fa/disable`、`POST /api/2fa/recovery/regenerate` 与 `POST /api/2fa/recovery/verify`。login 由 Rust 校验用户名/邮箱密码、账户锁定/停用状态和 2FA pending 分支，失败锁定阈值与时长读取 `BILL_ANALYSER_AUTH_MAX_LOGIN_ATTEMPTS` / `MAX_LOGIN_ATTEMPTS` 和 `BILL_ANALYSER_AUTH_LOCKOUT_DURATION_MINUTES` / `LOCKOUT_DURATION_MINUTES`，锁定与 last login 使用 UTC-naive 时间写入；成功时在同一 SQLite 事务内写入 access/refresh session、last login 与 `auth_logs`，并返回 Flask-compatible tokens、用户资料和 application cloud settings；2FA TOTP 登录验证会校验 `pending_2fa` action token 与当前 passcode，成功后写入新的 access/refresh session 和 `login_2fa_success` 日志。register 由 Rust 校验注册开关与密码策略，处理用户名/邮箱冲突，在同一 SQLite 事务内创建用户、注册预设分类、默认分类/规则、默认账户与 `auth_logs`，并返回 Flask-compatible 注册结果。Rust auth 响应会对现有前端 origin 返回 CORS 头，`OPTIONS /api/auth/login` 与 `OPTIONS /api/auth/register` 仍转发到 Python sidecar 以保留现有 preflight 行为；account-recovery/profile/cloud/external-auth/OAuth2 authorize/user-data statistics/export/clear/2FA status/2FA verify/2FA recovery verify/2FA write/step-up 新接管路由直接响应 `OPTIONS` preflight 或自身占位/统计/验证响应。token session 查询/撤销与个人 token 签发使用 Bearer access token、`sessions.token_hash`、用户启用状态和 session 过期时间解析当前用户与当前 session；API/MCP token 签发会校验当前密码、生成 HS256/HS384/HS512 HMAC JWT、写入 `sessions` 与 `auth_logs`，JWT `exp` 与 DB `expires_at` 来自同一过期 instant，响应中的 `apiBaseUrl` / `mcpUrl` 要求来自 `BILL_ANALYSER_PUBLIC_BASE_URL`，并按用户对当前密码失败尝试做短窗口限流。refresh token 交换现在由 Rust 校验 refresh JWT、`sessions.refresh_token_hash`、用户启用状态与 refresh 过期时间，并在同一事务内消耗旧 session 后轮换新的 access/refresh session，响应保持 Flask-compatible `success/result` 外壳且返回用户资料与 application cloud settings。logout 由 Rust 按 bearer token hash 失效 active session，并对重复 logout 或未知 token 保持 Flask-compatible 幂等成功。profile runtime 由 Rust 负责资料读取/更新、multipart 头像 data URL 存储/清空、验证邮件重发 mock-success auth log、应用云同步设置 key/type 校验与 full/partial upsert/delete、external-auth 列表/解绑、OAuth2 未绑定占位和 OAuth2 callback authorize disabled-safe/not-implemented 响应，并保留空 cloud settings 返回 `false` 的旧契约。user-data runtime 由 Rust 按当前 Bearer 用户统计数据、导出带 BOM 的 CSV/TSV，并通过当前密码、已配置的操作密码回退或 `step_up` action token 执行用户级交易/全量数据清理与 `user_data` 审计记录；2FA status runtime 由 Rust 按当前 Bearer 用户读取 `two_factor_enabled` 并返回 `enable/isEnabled`；2FA recovery verify runtime 由 Rust 校验 pending token、一次性消费哈希恢复码并写入登录 session/auth log/audit log；2FA write runtime 由 Rust 生成 setup secret/QR，确认启用时原子写入 secret/恢复码/session，并通过当前密码或 step-up token 禁用 2FA 或重生成恢复码；step-up runtime 由 Rust 通过当前密码、已配置的操作密码回退或已启用 2FA 的 TOTP passcode 签发一小时 `step_up` action token，并记录 `step_up_verified` auth log。OAuth provider exchange 在当前 workspace build 中是显式 disabled-safe/not-implemented 合同，不再保留 Python proxy。Rust foundation 还覆盖 Bearer header 解析、API/MCP/session token 类型投影、UA 设备名投影、注册密码策略校验、用户数据统计/导出/清理规范化、2FA 状态读取、2FA TOTP 登录验证、2FA 恢复码规范化与 `user_two_factor_recovery_codes` 的 hash/replace/consume/clear/count DB primitive，以及 2FA enable/disable 原子 DB primitive。
- Rust 迁移当前还具备运维安全合同层 `crates/bill-analyser-core/src/ops.rs`，固定 backup 文件名净化、`.zip` / `.zip.enc` 识别、恢复预验证响应、backup retention 计划、Fernet key 派生、SQLCipher 状态投影、用户数据清理审计 payload 与同步配置 secret redaction；`GET /api/settings/encryption/status` 已由 Rust HTTP runtime 直接输出该状态投影。Python/Flask 仍负责真实备份创建/删除/恢复、文件系统替换、SQLCipher 连接 patch 与备份域审计 DB 写入。
- 统计时间范围契约已收紧：`GET /api/statistics/category-statistics`、`GET /api/statistics/category-statistics/trends`、`GET /api/statistics/asset-trends` 在起始时间/年月晚于结束时间时返回 `400`，避免把反向区间静默视为空结果。
